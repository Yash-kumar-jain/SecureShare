<%- include('partials/header', { pageTitle: 'SecureShare - Home' }) %>

<div class="max-w-2xl mx-auto bg-white/80 backdrop-blur-sm rounded-2xl shadow-xl overflow-hidden p-8 text-center mt-12 border border-white/20">
    <div class="mb-8">
        <div class="w-32 h-32 bg-gradient-to-r from-gradient-start to-gradient-end rounded-full flex items-center justify-center mx-auto mb-6 shadow-lg">
            <i class="fas fa-share-alt text-5xl text-white"></i>
        </div>
        <h1 class="text-4xl font-bold text-gray-800 mb-4">Secure P2P File Sharing</h1>
        <p class="text-lg text-gray-600 mb-6">Send files directly between devices with end-to-end encryption</p>
    </div>

    <a href="/create" class="inline-block px-8 py-4 bg-gradient-to-r from-gradient-start to-gradient-end text-white rounded-xl hover:shadow-lg transition-all duration-300 transform hover:scale-105 text-lg font-medium">
        <i class="fas fa-bolt mr-2"></i> Start New Transfer
    </a>

    <div class="mt-12 pt-8 border-t border-gray-200/50">
        <h3 class="text-xl font-semibold text-gray-700 mb-6">How it works</h3>
        <div class="grid md:grid-cols-3 gap-6">
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
                <div class="w-16 h-16 bg-gradient-to-br from-primary-100 to-primary-200 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span class="text-2xl font-bold text-primary-800">1</span>
                </div>
                <h4 class="font-medium text-gray-800 mb-2">Create Transfer</h4>
                <p class="text-sm text-gray-500">Generate a secure link to share</p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
                <div class="w-16 h-16 bg-gradient-to-br from-primary-100 to-primary-200 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span class="text-2xl font-bold text-primary-800">2</span>
                </div>
                <h4 class="font-medium text-gray-800 mb-2">Share Link</h4>
                <p class="text-sm text-gray-500">Send it to your recipient</p>
            </div>
            <div class="bg-white p-6 rounded-xl shadow-sm border border-gray-100">
                <div class="w-16 h-16 bg-gradient-to-br from-primary-100 to-primary-200 rounded-full flex items-center justify-center mx-auto mb-4">
                    <span class="text-2xl font-bold text-primary-800">3</span>
                </div>
                <h4 class="font-medium text-gray-800 mb-2">Transfer Files</h4>
                <p class="text-sm text-gray-500">Send files directly peer-to-peer</p>
            </div>
        </div>
    </div>
</div>

<%- include('partials/footer') %>